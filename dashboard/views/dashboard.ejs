<!DOCTYPE html>
<html>
<head>
    <title>LostSignal Dashboard</title>
    <style>
        body { font-family: sans-serif; background: #1a1a1a; color: white; padding: 20px; }
        h1 { color: #ad5eff; }
        input, textarea, select {
            padding: 10px;
            margin-top: 8px;
            display: block;
            width: 100%;
            max-width: 400px;
            background: #2a2a2a;
            border: 1px solid #444;
            color: white;
            border-radius: 5px;
        }
        button {
            background: #ad5eff;
            border: none;
            padding: 10px 20px;
            margin-top: 12px;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
        }
        a { color: #ad5eff; }
    </style>
</head>
<body>
    <h1>📬 Send Mail</h1>
    <p>Logged in as <strong><%= user.username %></strong> | <a href="/logout">Logout</a></p>

    <form action="/dashboard/mail" method="POST">
        <label for="channel">Channel ID:</label>
        <input type="text" name="channel" required placeholder="e.g. 123456789012345678">

        <label for="message">Message Text:</label>
        <textarea name="message" rows="5" required placeholder="Type your message here..."></textarea>

        <button type="submit">Send Mail</button>
    </form>

    <% if (success) { %>
        <p style="color: lightgreen;">✅ Mail sent!</p>
    <% } else if (error) { %>
        <p style="color: salmon;">❌ <%= error %></p>
    <% } %>
</body>
</html>
<hr>
<h2>📢 Set Announcement Channel</h2>
<form action="/dashboard/announcement" method="POST">
    <label for="type">Announcement Type:</label>
    <select name="type" required>
        <option value="welcome">Welcome</option>
        <option value="goodbye">Goodbye</option>
        <option value="boost">Boost</option>
    </select>

    <label for="channel">Channel ID:</label>
    <input type="text" name="channel" placeholder="e.g. 123456789012345678" required>

    <button type="submit">Update Announcement</button>
</form>

